name: soil_temp_monitor
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^2.0.0
  permission_handler: ^11.3.1
  syncfusion_flutter_charts: ^31.2.2
  google_fonts: ^5.0.0
  shared_preferences: ^2.2.2
  share_plus: ^12.0.1
  http: ^1.2.1
  path_provider: ^2.1.5
  intl: ^0.18.1
  fl_chart: ^0.66.0
  google_generative_ai: ^0.4.3
  # New dependencies for notifications and sound
  audioplayers: ^5.2.1
  flutter_local_notifications: ^17.2.3

  url_launcher: ^6.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add sound asset for alerts
  assets:
    - assets/sounds/alert.mp3
